# 需求附件管理验收说明

本文档用于验证需求附件在草稿、确认版本和历史版本之间的关联规则。

## 验收检查

- 保存草稿时，附件集合按请求内容完整替换。
- 确认草稿后，附件引用复制到新版本。
- 后续修改草稿附件不会影响已确认版本。
- 点击附件行会打开文件标签页，并保留当前需求标签页。

## 边界

单个草稿最多关联 10 个附件。跨应用文件、待上传文件和重复引用都必须返回明确错误，且不产生部分写入。
